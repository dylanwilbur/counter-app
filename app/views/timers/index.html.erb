<h1>Timers</h1>

<ul id=timers>
  <% @timers.each do |timer| %>
    <li><%= link_to "Timer number", timer_path(timer), class: "btn btn-secondary" %> <%= timer.id %>: <%= timer.elapsed_time %></li>
  <% end %>
</ul>