<h1>Timers</h1>

<ul id=timers>
  <% @timers.each do |timer| %>
    <li><%= link_to "Timer #{timer.id}", timer_path(timer), class: "btn btn-secondary" %> : <%= timer.elapsed_time %></li>
  <% end %>
</ul>